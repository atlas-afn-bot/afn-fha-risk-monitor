# NOTE: infra/snapshot-pipeline/container/requirements.txt is the pinned
# source of truth for the deployed container image. This file has looser
# bounds for local-dev convenience; keep the package set in sync (adds/removes)
# but pins may differ.
openpyxl>=3.1
pandas>=2.0
openai>=1.40
# xlrd 2.x supports legacy .xls only (BIFF). Needed for older HUD exports
# that the RPA still ships as .xls (e.g. 2026-05 uploads).
xlrd>=2.0,<3.0
